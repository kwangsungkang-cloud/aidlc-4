spring:
  datasource:
    url: jdbc:mysql://localhost:3306/tableorder?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: tableorder
    password: tableorder123
  data:
    redis:
      host: localhost
      port: 6379
      ssl:
        enabled: false
  jpa:
    properties:
      hibernate:
        format_sql: true
    show-sql: true

app:
  cors:
    allowed-origins: http://localhost:5173,http://localhost:5174
  s3:
    bucket: tableorder-menu-images-dev

logging:
  level:
    com.tableorder: DEBUG
    org.hibernate.SQL: DEBUG
